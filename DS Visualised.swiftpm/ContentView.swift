import SwiftUI
import Collections
struct ContentView: View {
    var body: some View {
        ZStack {
            //Color.gray.ignoresSafeArea()
            VStack {
                MainMenuView()
            }
        }
    }
}
